{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: api
  name: api
spec:
  ports:
  - name: "http"
    protocol: TCP
    port: {{ .Values.api.port }}
    targetPort: {{ .Values.api.port }}
  selector:
    app: api
  type: ClusterIP
status:
{{- end }}
